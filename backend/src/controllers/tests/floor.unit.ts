import { expect } from 'chai';
import request from 'supertest';

describe('Floor controller', () => {
  

  it('should create a floor', async () => {
    
  });

  it('should update a floor', async () => {
    
  });

  it('should get a floor', async () => {
    
  });  
});
