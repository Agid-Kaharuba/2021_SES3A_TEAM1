# Stage to run the application as [dev|test|prod]
STAGE=dev

# Port to run API on
API_PORT=4000

DATABASE_URL=<ENTER CONNECTION STRING>
DATABASE_NAME=master

# Key for auth 
TOKEN_SECRET=<ENTER TOKEN STRING>